# 交付说明

智能 AI Agent 售前服务系统 · 交付状态 = **可运行、可测试、可演示**。

## 一、交付内容

| 类别 | 内容 |
| --- | --- |
| 设计文档 | `../智能AI-Agent售前服务系统-设计文档.md` |
| 六大模块 | `app_core` / `data_pipeline` / `retrieval` / `agents` / `frontend` |
| 服务编排 | `service.py`（统一装配入口） |
| API 网关 | `api/`（FastAPI,REST 接口 + 托管前端） |
| Web 前端 | `frontend/`（Vue 3 + Vite + Element Plus 深色 + ECharts,五页） |
| 端到端演示 | `demo.py` |
| 单元测试 | `tests/`（unittest，52 例，含 G-2 检索评测 / G-4 会话隔离 / REST 网关） |
| 测试入口 | `run_tests.py` |
| 配置 | `config.yaml`（非敏感）+ `.env.example`（敏感模板） |
| 依赖说明 | `requirements.txt` / `requirements-full.txt` |

## 二、运行方式

```bash
cd C:/Users/Lenovo/agent-system

python demo.py        # 端到端演示（零依赖、无需 API Key）
python run_tests.py   # 52 个单元测试（全部离线）
# Web 前端:双击 启动前端.bat(首次自动构建),或:
python -m uvicorn api.main:app --host 127.0.0.1 --port 8000   # http://127.0.0.1:8000
```

## 三、⚠️ LLM 接入点（此处留空，由你决策）

**当前 LLM 模块处于「留空」状态**：未接任何真实模型，运行时自动使用离线
Mock（模板化话术），保证无 Key 也能端到端跑通。

要接入真实模型，**只做一处决策、只改一个文件，无需改任何代码**：

1. 复制模板：`cp .env.example .env`
2. 在 `.env` 里填三项：
   ```
   LLM_MODEL=你的模型名
   LLM_API_KEY=你的密钥
   LLM_BASE_URL=你的 OpenAI 兼容接口地址
   ```

决策点仅为「选哪个模型 / 哪个服务商」。填好后，`demo.py`、前端「智能问答」
和「内容生成」三处会自动切到真实生成，其余逻辑不变。

> 需要切换向量后端（simple → chroma）时，另在 `.env` 加
> `RETRIEVAL_VECTOR_BACKEND=chroma` 并 `pip install chromadb`——这与 LLM 无关，属可选。

## 四、验收状态

### 已验收（单元测试全绿）

- FR-01 企业框架：强类型模型 / 统一日志 / 全局异常降级 / `python -m app_core` 自检（AC-01.1）
- FR-02 混合检索：**LangGraph 已接入**；FTS5 + 向量双路可查、融合排序；评测集命中率 100% ≥85%（G-2）
- FR-03 数据管道：清洗 / 去重 / 会话切分 / PII 脱敏（样例数据 100%）
- FR-04 售前 Agent：**LangGraph StateGraph 实现 ReAct**、LangChain Tool 封装、失败降级、超步终止（G-5 故障率 0）
- FR-05 内容 Agent：**LangGraph 多阶段工作流**、失败跳过、Checkpointer 断点恢复
- FR-06 前端：五页渲染（总览/问答/入库/生成/管道）+ **会话新建/切换隔离**（G-4），Vue 3 + FastAPI
- FR-07 整体组装：`demo.py` 全链路跑通 + 检索单/双引擎对比评测

### 待你验证（需要真实数据/真实模型）

| 指标 | 现状 |
| --- | --- |
| G-2 检索准确率 ≥85% | 离线评测集实测 100%（18/18，见 `docs/检索评测报告.md`）；真实客服数据待复验 |
| G-3 脱敏准确率 100% | 样例验证通过（手机/身份证/邮箱），未跑真实数据 |
| 真实问答/生成效果 | 依赖第三步 LLM 接入后评估 |

## 五、已知边界

- Agent 状态机**已用官方 LangGraph StateGraph 编排**（LangChain 1.x + LangGraph，满足 PRD 2.2 技术栈锁定）。
- 检索后端默认 `chroma`（ChromaDB），未安装 chromadb 时自动回退内置 `simple` 后端；embedding 默认内置特征哈希，填入 `EMBEDDING_MODEL` 后走真实语义向量（见 `.env.example`）。